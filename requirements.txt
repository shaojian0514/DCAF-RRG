# DCAF: Disease-Consistent Visual-Text Alignment and Fusion
# for Reliable Radiology Report Generation
#
# Python >= 3.8 recommended

# ── Core Deep Learning ────────────────────────────────────────────────────────
torch>=1.12.0
torchvision>=0.13.0

# ── Transformers & NLP ────────────────────────────────────────────────────────
transformers==4.25.1
tokenizers>=0.13.2

# ── CLIP (for retrieval queue construction) ───────────────────────────────────
# Install via: pip install git+https://github.com/openai/CLIP.git
# or: pip install openai-clip
openai-clip

# ── Scientific Computing ──────────────────────────────────────────────────────
numpy>=1.21.0
scipy>=1.7.0

# ── Image Processing ──────────────────────────────────────────────────────────
Pillow>=9.0.0
opencv-python>=4.5.0

# ── Evaluation Metrics ────────────────────────────────────────────────────────
# pycocoevalcap is included in the repo (pycocoevalcap/)
# Java is required for METEOR evaluation (meteor-1.5.jar)
nltk>=3.7

# ── CheXbert (Clinical Evaluation) ───────────────────────────────────────────
# CheXbert checkpoint must be downloaded separately:
# https://stanfordmedicine.app.box.com/s/c3stck6w6dol3h36grdc97xoydzxd7w9
# Place at: checkpoints/stanford/chexbert/chexbert.pth

# ── Utilities ─────────────────────────────────────────────────────────────────
tqdm>=4.64.0
pandas>=1.3.0
matplotlib>=3.5.0
tensorboard>=2.9.0

# ── Distributed Training ──────────────────────────────────────────────────────
# torch.distributed is included with PyTorch
# For multi-node: install NCCL (usually bundled with CUDA)

# ── Optional: Faster tokenization ────────────────────────────────────────────
sentencepiece>=0.1.96
